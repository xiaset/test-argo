apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-template.fullname" . }}-test-connection"
  labels:
{{ include "chart-template.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "chart-template.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
